<h1>Posts</h1>

<% @posts.each do |post| %>
  <% if user_signed_in? %>
    <li>
      <h2><%= current_user.name %></h2>
      <p><%= post.body %></p>
    </li>
  <% else %>
    <li>
      <p><%= post.body %></p>
    </li>
  <% end %>
<% end %>

<%= link_to "Create new post", new_post_path %>